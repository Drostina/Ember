[customizations.installer.kickstart]
contents = """
%post
bootc switch --mutate-in-place --enforce-container-sigpolicy --transport registry ghcr.io/drostina/ember:latest
%end

%post --nochroot --interpreter=/usr/bin/bash --log=/tmp/ember-flatpak-install.log
if ! unshare --mount --propagation private /usr/bin/bash /mnt/sysroot/usr/libexec/ember-flatpak-anaconda; then
    echo "Ember Flatpak setup will resume automatically after reboot."
fi
%end
"""

[customizations.installer.modules]
enable = [
  "org.fedoraproject.Anaconda.Modules.Network",
  "org.fedoraproject.Anaconda.Modules.Storage",
  "org.fedoraproject.Anaconda.Modules.Runtime"
]
disable = [
  "org.fedoraproject.Anaconda.Modules.Security",
  "org.fedoraproject.Anaconda.Modules.Services",
  "org.fedoraproject.Anaconda.Modules.Users",
  "org.fedoraproject.Anaconda.Modules.Subscription",
  "org.fedoraproject.Anaconda.Modules.Timezone"
]
